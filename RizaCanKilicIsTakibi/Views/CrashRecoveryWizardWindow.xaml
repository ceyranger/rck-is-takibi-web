<Window x:Class="RizaCanKilicIsTakibi.Views.CrashRecoveryWizardWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Kurtarma Sihirbazı"
        Width="640"
        Height="480"
        MinWidth="520"
        MinHeight="360"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResizeWithGrip"
        Background="{StaticResource Brush.AppBackground}">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Kurtarma Sihirbazı"
                   FontFamily="{StaticResource Font.Primary}"
                   FontSize="24"
                   FontWeight="SemiBold"
                   Margin="0,0,0,8"/>

        <StackPanel Grid.Row="1" Margin="0,0,0,12">
            <TextBlock Text="Program beklenmedik şekilde kapanmış olabilir. Kaydedilmemiş oturum bulundu."
                       TextWrapping="Wrap"
                       Foreground="{StaticResource Brush.TextSecondary}"/>
            <TextBlock Margin="0,8,0,0" TextWrapping="Wrap">
                <Run Text="Son başarılı kayıt: "/>
                <Run Text="{Binding LastSuccessfulSaveText}" FontWeight="SemiBold"/>
            </TextBlock>
            <TextBlock Margin="0,4,0,0" TextWrapping="Wrap">
                <Run Text="Kurtarma anı: "/>
                <Run Text="{Binding RecoveryCreatedText}" FontWeight="SemiBold"/>
            </TextBlock>
        </StackPanel>

        <Border Grid.Row="2"
                Style="{StaticResource CardBorderStyle}"
                Padding="12">
            <DockPanel>
                <TextBlock DockPanel.Dock="Top"
                           Text="Yapılan değişiklikler"
                           FontWeight="SemiBold"
                           Margin="0,0,0,8"/>
                <ListBox ItemsSource="{Binding ChangeLines}"
                         BorderThickness="0"
                         Background="Transparent"
                         ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding}"
                                       TextWrapping="Wrap"
                                       Margin="0,0,0,8"/>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </DockPanel>
        </Border>

        <StackPanel Grid.Row="3"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,16,0,0">
            <Button Content="Kapat"
                    Width="110"
                    Margin="0,0,8,0"
                    Style="{StaticResource GhostButton}"
                    Command="{Binding DiscardCommand}"/>
            <Button Content="Kurtar"
                    Width="120"
                    Command="{Binding RecoverCommand}"/>
        </StackPanel>
    </Grid>
</Window>
