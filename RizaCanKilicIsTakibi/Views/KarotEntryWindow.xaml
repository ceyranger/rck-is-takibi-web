<Window x:Class="RizaCanKilicIsTakibi.Views.KarotEntryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:behaviors="clr-namespace:RizaCanKilicIsTakibi.Behaviors"
        xmlns:controls="clr-namespace:RizaCanKilicIsTakibi.Views.Controls"
        Title="Karot Kaydı Ekle"
        Width="620"
        Height="560"
        MinWidth="560"
        MinHeight="480"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResizeWithGrip"
        Background="{StaticResource Brush.AppBackground}">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Yeni Karot Kaydı"
                   FontFamily="{StaticResource Font.Primary}"
                   FontWeight="SemiBold"
                   FontSize="22"
                   Margin="0,0,0,10" />

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
            <StackPanel>
                <TextBlock Text="Proje (isteğe bağlı)" FontWeight="SemiBold" />
                <controls:ProjectPickerControl Margin="0,4,0,8"
                                               SelectedProjectId="{Binding SelectedProjectId, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                               Catalog="{Binding CatalogEntries}" />

                <TextBlock Text="Numune Alınma Tarihi" FontWeight="SemiBold" />
                <DatePicker Margin="0,4,0,8"
                            SelectedDate="{Binding SampleReceivedDate, Mode=TwoWay}" />

                <TextBlock Text="Ada Parsel" FontWeight="SemiBold" />
                <TextBox Margin="0,4,0,8" Text="{Binding AdaParsel, UpdateSourceTrigger=PropertyChanged}" />

                <TextBlock Text="Yapı Sahibi" FontWeight="SemiBold" />
                <TextBox Margin="0,4,0,8" Text="{Binding YapiSahibi, UpdateSourceTrigger=PropertyChanged}" />

                <TextBlock Text="YİBF No" FontWeight="SemiBold" />
                <TextBox Margin="0,4,0,8" Text="{Binding YibfNo, UpdateSourceTrigger=PropertyChanged}" />

                <TextBlock Text="Müteahhit" FontWeight="SemiBold" />
                <TextBox Margin="0,4,0,8" Text="{Binding Muteahhit, UpdateSourceTrigger=PropertyChanged}" />

                <TextBlock Text="Kat Bilgisi" FontWeight="SemiBold" />
                <TextBox Margin="0,4,0,8" Text="{Binding KatBilgisi, UpdateSourceTrigger=PropertyChanged}" />

                <TextBlock Text="Beton Sınıfı" FontWeight="SemiBold" />
                <TextBox Margin="0,4,0,8" Text="{Binding BetonSinifi, UpdateSourceTrigger=PropertyChanged}" />

                <TextBlock Text="28 Günlük Sonuç" FontWeight="SemiBold" />
                <TextBox Margin="0,4,0,8" Text="{Binding TwentyEightDayResult, UpdateSourceTrigger=PropertyChanged}" />

                <TextBlock Text="Beton Firması" FontWeight="SemiBold" />
                <TextBox Margin="0,4,0,8" Text="{Binding BetonFirmasi, UpdateSourceTrigger=PropertyChanged}" />

                <TextBlock Text="Laboratuvar" FontWeight="SemiBold" />
                <TextBox Margin="0,4,0,8" Text="{Binding Laboratuvar, UpdateSourceTrigger=PropertyChanged}" />

                <TextBlock Text="Açıklama" FontWeight="SemiBold" />
                <TextBox Margin="0,4,0,8"
                         MinHeight="80"
                         AcceptsReturn="True"
                         TextWrapping="Wrap"
                         VerticalContentAlignment="Top"
                         VerticalScrollBarVisibility="Auto"
                         behaviors:TextBoxMouseWheelBehavior.IsEnabled="True"
                         Text="{Binding Aciklama, UpdateSourceTrigger=PropertyChanged}" />

                <TextBlock Text="{Binding ValidationMessage}"
                           Foreground="{StaticResource Brush.Danger}"
                           Margin="0,2,0,4"
                           TextWrapping="Wrap" />
            </StackPanel>
        </ScrollViewer>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="İptal"
                    Style="{StaticResource GhostButton}"
                    Width="96"
                    Margin="0,0,8,0"
                    Command="{Binding CancelCommand}" />
            <Button Content="Kaydet"
                    Width="120"
                    Command="{Binding SaveCommand}" />
        </StackPanel>
    </Grid>
</Window>
