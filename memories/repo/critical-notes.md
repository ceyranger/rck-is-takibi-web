# Critical Notes

- Canonical daily-use executable: `RizaCanKilicIsTakibi\bin\Release\publish\RizaCanKilicIsTakibi.exe`.
- Live data is stored beside that executable under `Data\tasks.db`, `Data\last-save.json`, `Backup\`, and `Logs\`; publish and cleanup work must preserve these paths.
- This workspace has a `.git` directory. After meaningful code changes, run tests and create a Git commit checkpoint.
- For release delivery, publish to a temporary folder first, then copy only application output into `bin\Release\publish` after confirming `RizaCanKilicIsTakibi.exe` is not running.
- 2026-06-10: YİBF ana bilgi olay silme bug fix completed. Live publish exe was updated and `Data\tasks.db` / `Data\last-save.json` hashes were verified unchanged after publish.
- 2026-06-10: Tadilat Takibi, YİBF Ana Bilgi, and YİBF İş Takibi row reorder commands completed. Live publish exe was updated after full tests; `Data\tasks.db` and `Data\last-save.json` hashes were verified unchanged.
- 2026-06-10: `TÜM EKSİKLER` tab completed. It is read-only, uses YİBF Ana Bilgi as the primary grouping source, and shows unmatched YİBF İş Takibi/Tadilat/Eksik Proje/Karot records separately instead of guessing identity matches. Live publish `Data`, `Backup`, and `Logs` remain protected during build cleanup and publish updates.
- 2026-06-10: `TÜM EKSİKLER` Karot items now include visible `Kat Bilgisi: ...` text in the deficiency reason. Full tests passed and live publish data hashes stayed unchanged.
- 2026-06-10: `TÜM EKSİKLER` deficiency rows now show a separate `Satır: ...` source context line built only from non-empty source columns. Search includes this context. Full tests passed and live publish data hashes stayed unchanged.
- 2026-06-10: Tadilat Takibi scroll performance work completed. The view now uses one virtualized `DisplayRows` list instead of nested district row lists, preserving district labels and empty-district add rows. Full tests passed and live publish data hashes stayed unchanged.
- 2026-06-10: YİBF Ana Bilgi / YİBF İş Takibi work identity fields were added (`WorkGroupId`, `WorkIdentityId`, `WorkVariantLabel`). SQLite migration creates a pre-migration backup under `Backup\schema-migration-yibf-work-id-*` before altering old YİBF schemas. Full tests passed, live publish exe was updated, and `Data\tasks.db` / `Data\last-save.json` hashes stayed unchanged.
- 2026-06-10: Tadilat Takibi scroll regression fixed by returning the screen to the single virtualized `DisplayRows` list. The district label is visible on each row to avoid blank district cells; do not reintroduce nested district row controls for this screen. The Tadilat list must use pixel scroll, not `VirtualizingPanel.ScrollUnit="Item"`, because item scrolling makes small wheel movement jump too far. Full tests passed, live exe was updated after the app closed, and `Data\tasks.db` / `Data\last-save.json` hashes stayed unchanged.
- 2026-06-11: Genel İş Takibi / Acil İşler quick task templates completed. Templates live in SQLite `QuickTaskTemplates`, are included in JSON backup schema v2 with v1 checksum compatibility, and bulk quick add inserts selected templates at the top of Acil İşler as one undo/redo action. Full tests passed, live publish exe was updated, and `Data\tasks.db` / `Data\last-save.json` size/timestamps stayed unchanged.
- 2026-06-11: Quick task templates changed to grouped user-defined lists. The app no longer seeds built-in quick templates; the six prior built-in titles are soft-deleted once on startup if present. `GroupName` is included in backup schema v2 and older v2 quick-template backups remain checksum-compatible. Full tests passed, live publish exe was updated, and live data hashes stayed unchanged during publish.
- 2026-06-11: Non-invasive bug audit completed without updating canonical publish. Release build and tests passed (147/147), live `Data\tasks.db` and `Data\last-save.json` stayed unchanged. NuGet vulnerable-package scan found no vulnerable packages in the app project, but the test project has high advisory transitive packages `System.Net.Http 4.3.0` and `System.Text.RegularExpressions 4.3.0` through old test dependencies.
- 2026-07-18: Search UX refresh completed. New `ARAMA` tab (`MainNavigationTab.Arama`), full-page `SearchSectionView`, tab-local Ctrl+F overlay with locked scope, project toolbar strips removed from module sections. Release tests passed 157/157.
- 2026-07-22: Proje Kataloğu İstinat ekleme: üst proje seçimi görünmüyor / `Guid?` ComboBox bağlama kırığı düzeltildi. Dialogda İstinat seçilince `ProjectPickerControl` ile zorunlu üst proje alanı açılıyor; Normal üst yoksa uyarı gösteriliyor. Release tests 173/173. Canonical publish exe güncellendi; `Data\tasks.db` ve `Data\last-save.json` SHA256/size/timestamp değişmedi; `Backup` ve `Logs` korundu.
- 2026-07-23: YİBF İş Takibi araması Tadilat tarzına alındı: modal overlay kaldırıldı, araç çubuğunda anlık filtreleyen arama kutusu eklendi. Release tests 175/175. Canonical publish exe güncellendi; `Data\tasks.db` / `Data\last-save.json` hash/size/timestamp değişmedi.
- 2026-07-23: Sol panel `ARAMA` butonu YİBF İş Takibi altına taşındı. Excel/PDF paket çıktılarına header band, zebra, kenarlık, AutoFilter ve PDF sayfa numarası + hücre rengi taşıma eklendi. Release tests 177/177. Canonical publish güncellendi; canlı Data hash değişmedi.
- 2026-07-23: Ana Bilgi olay diyaloğuna bugün varsayılan tarih + Bugün butonu ve isteğe bağlı onay kategorisi eklendi. Proje Onay Takibi kategori filtreleri, geçen gün ve 7+ gün uyarısı ile yenilendi; Onaylanan listeden düşüyor. Release tests 181/181. Canonical publish güncellendi; `Data\tasks.db` / `Data\last-save.json` hash/size/timestamp değişmedi.
- 2026-07-23: Proje Onay Takibi kartına çift tıklama ilgili Ana Bilgi olay düzenleme penceresini açıyor. Release tests 182/182. Canonical publish güncellendi; canlı Data hash değişmedi.
- 2026-07-23: Proje Onay Takibi düzenleme reentrancy çökmesi düzeltildi; kaydedilmemiş oturum için Kurtarma Sihirbazı eklendi (`Data\pending-recovery.json`). Release tests 184/184. Canonical publish güncellendi; canlı Data hash değişmedi.
- 2026-07-23: Ana Bilgi olaylarına **Pasif** kategorisi (gri) eklendi; tüm kategorilerde elle renk ataması açıldı (kategori varsayılan rengi önerir, override kaydedilir). Pasif, Onaylanan gibi Proje Onay Takibi listesinden düşer. Release tests 184/184. Canonical publish güncellendi; `Data\tasks.db` / `Data\last-save.json` hash/size/timestamp değişmedi.
- 2026-07-23: Ana Bilgi kategori varsayılan renkleri: İncelenecek kırmızı, Denetçiden dönüş turuncu (`#FFFFA500`), Müelliften revize sarı; Onaylanan/Pasif aynı. Renk listesine Turuncu eklendi. Release tests 184/184; publish Data hash korundu.
- 2026-07-23: Ana Bilgi **Beklenen** kategorisi eklendi (sarı `#FFFFFF00`); Proje Onay Takibi’nde bekleyen olarak görünür ve filtre chip’i var. Release tests 184/184; canonical publish güncellendi; Data hash korundu.
- 2026-07-23: Ana Bilgi UX 1–6: kategori=durum / renk=öncelik (kırmızı-turuncu-sarı); Beklenen kimlik rengi lila `#FFB39DDB`; Proje Onay kartında büyük kategori + sol öncelik şeridi; aynı projede çoklu bekleyen olay ayrı kart. Gecikme 7 gün. Release tests 186/186; publish Data hash korundu.
- 2026-07-23: Proje Onay Takibi **proje grubu**: aynı ada/parsel altındaki tüm bekleyen olaylar tek kartta; kategori renkleri kırmızı→gri (Beklenen `#FFE8E0A8`, Pasif `#FF9E9E9E`); Öncelik alanı kaldırıldı, renk kategoriden. Release tests 186/186; publish Data hash korundu.
- 2026-07-23: Proje Onay kategori filtresi grup içi satırları daraltır: Tümü=tüm olaylar; kategori seçilince yalnızca o kategorideki satırlar (eşleşmeyen grup gizlenir). Release tests 186/186; publish Data hash korundu.
- 2026-07-29: Karot “Kayıt Ekle” diyaloğuna eksik grid sütunları eklendi (Numune Alınma Tarihi, 28 Günlük Sonuç, Beton Firması, Laboratuvar). Tests 187/187. Canonical publish exe güncellendi; `Data\tasks.db` / `Data\last-save.json` hash/size/timestamp değişmedi; `Backup` ve `Logs` korundu.
- 2026-07-29: Kayıt Ekle Soft Cards + proje özeti: paylaşılan `DialogFieldStyles`, Karot/Tadilat/Ana Bilgi/İş Takibi/Eksik Proje/Aksiyon diyalogları; proje seçiminde kimlik otomatik + Elle düzenle. Tests 190/190. Canonical publish exe güncellendi; `Data\tasks.db` / `Data\last-save.json` hash/size/timestamp değişmedi; `Backup` ve `Logs` korundu.
- 2026-07-29: Entry dialog identity-loss fix: `BuildEntry` VM kimliğini korur; eksik katalogda “Projeden kullan” manuel değerleri silmez. Tests 191/191. Canonical publish exe güncellendi; `Data\tasks.db` / `Data\last-save.json` hash/size/timestamp değişmedi; `Backup` ve `Logs` korundu.
- 2026-07-29: P2 yerinde arama filtreleri Aksiyon, Eksik Proje, Karot, Genel İş Takibi, Acil İş Özeti ve Proje Onay Takibi yüzeylerine eklendi. Solution build başarılı, tests 192/192; canonical publish güncellenmedi.
- 2026-07-29: Yol haritası P4–P7 tamamlandı: bağlantı sağlığı metriği/çözüm yönlendirmesi, katalogdan bağlı kimlik alanlarını onaylı overwrite, olumsuz karottan Aksiyon taslağı ve ortak ilçe kataloğu eklendi. `MERKEZ`/`SİNOP` yalnız filtre aliasıdır; saklanan ilçe adı değiştirilmez. Solution build 0 hata/uyarı, tests 198/198; canonical publish ve canlı veri klasörleri güncellenmedi.
- 2026-07-29: Yol haritası P1–P7 doğrulandı ve teslim edildi (`7fa09ee`). Tests 198/198. Canonical publish exe güncellendi; `Data\tasks.db` / `Data\last-save.json` hash/size/timestamp değişmedi; `Backup` ve `Logs` korundu.
- 2026-07-29: Karot sekmesi hücre etkileşimi Tadilat ile hizalandı: tek tık satır seçimi, çift tık / F2 hücre düzenleme; Kaydet/kapanışta açık draft commit. Tests 200/200; canonical publish henüz güncellenmedi.
- 2026-07-29: YİBF Ana Bilgi görünen adı **Proje Takibi**; sol iş listesi `0.35*` (MinWidth 260), sağ bilgi `0.65*`. Excel sheet `Proje Takibi` (import eski `ANA BİLGİ` fallback). Enum/DB değişmedi. Tests 200/200; canonical publish henüz güncellenmedi.
- 2026-07-29: Proje Takibi + Karot hücre edit teslim edildi (`9b625d2`). Canonical publish exe güncellendi; `Data\tasks.db` / `Data\last-save.json` hash/size/timestamp değişmedi; `Backup` ve `Logs` korundu.
- 2026-07-29: Ayarlar layout düzeltildi (katalog `*`, üst araçlar ScrollViewer); tab host `Grid.Column=1` kaldırıldı. Tek ortak mavi Kaydet (sol panel + Ctrl+S → SaveAll). “Çözülmemiş bağlantı” metriği netleştirildi (kaydetten bağımsız DryRun). Tests 200/200; canlı Data’ya dokunulmadı; publish henüz yok.
- 2026-07-29: Ortak Kaydet + Ayarlar UX commit `46f07e3`. Canonical publish bekliyor — canlı exe açık (Data hash korunacak).
- 2026-07-29: Ortak Kaydet + Ayarlar UX teslim (`46f07e3`). Canonical publish exe güncellendi; `Data\tasks.db` / `Data\last-save.json` hash/size/timestamp değişmedi; `Backup` ve `Logs` korundu.
- 2026-07-29: Çözülmemiş bağlantı Apply+Kaydet sonrası düşmeme bug’ı: `CloneEntry` `ProjectId` kopyalamıyordu (Karot/Aksiyon/Tadilat/Eksik). Fix + tests 203/203; publish henüz yok. Eski Apply’lar DB’de olmayabilir — yeniden uygula+kaydet gerekir.
- 2026-07-29: ProjectId snapshot fix teslim (`d6ac691`). Canonical publish exe güncellendi; `Data\tasks.db` / `Data\last-save.json` hash/size/timestamp değişmedi. Kullanıcı isteği: anlamlı iş sonrası sormadan commit+güvenli publish (`.cursor/rules/auto-commit-publish.mdc`).
- 2026-08-03: YİBF İş Takibi **Satır Ekle** crash: `Belediye`/`Muteahhit` private set + TextBox TwoWay → `InvalidOperationException` → fatal shutdown. Fix `Mode=OneWay` commit `e1135b1`; tests 203/203. Canonical publish exe güncellendi; `Data\tasks.db` / `Data\last-save.json` hash/size/timestamp değişmedi.
- 2026-08-05: Karot İstinat bağlama: proje seçici üst kimlikle İstinat bulur; Karot doldurma üstten miras + boş Muteahhit←YapiSahibi (Normal/İstinat). Tests 211/211. Commit `4a41d01`; canonical publish exe güncellendi; `Data\tasks.db` / `Data\last-save.json` hash/size/timestamp değişmedi; `Backup` ve `Logs` korundu.
- 2026-08-05: Tüm paylaşılan proje seçicilerde İstinat ayrı başlık (`İstinat · sahip`) + aile sıralaması; Tadilat/Aksiyon/Eksik/YİBF İş Takibi Apply etkin kimlik kullanır. Ana Bilgi İstinat dışlaması bilinçli. Tests 214/214.
