window.WEB_VIEWER_CONFIG = {
  // Google Apps Script Web App deployment URL (ends with /exec)
  appsScriptUrl: "",
  // Optional: set true to skip PIN gate when loading local sample JSON
  allowLocalSample: false
};
